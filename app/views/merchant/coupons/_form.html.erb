<%= form_for([current_user.merchant, @coupon]) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name%>

  <%= f.label :code %>
  <%= f.text_field :code %>

  <%= f.label :percent %>
  <%= f.number_field :percent, min: 0, max: 100 %>

  <%= f.submit %>
<% end %>
